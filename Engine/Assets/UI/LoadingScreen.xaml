<Page
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  mc:Ignorable="d"
  d:DesignWidth="1920"
  d:DesignHeight="1080"
  Background="#FF4D5052">
  <Grid Background="#FF000000">
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="1445*"/>
      <ColumnDefinition Width="255*"/>
      <ColumnDefinition Width="173*"/>
      <ColumnDefinition Width="47*"/>
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
      <RowDefinition Height="900*"/>
      <RowDefinition Height="114*"/>
      <RowDefinition Height="66*"/>
    </Grid.RowDefinitions>

    <Grid.Resources>
      <!-- ANIMACION DE SALIDA -->
      <Storyboard x:Key="ExitStoryboard">
        <!-- Imagen: slide hacia abajo + fade out -->
        <DoubleAnimation
          Storyboard.TargetName="ImageSlide"
          Storyboard.TargetProperty="Y"
          From="0" To="40"
          Duration="0:0:0.5">
          <DoubleAnimation.EasingFunction>
            <CubicEase EasingMode="EaseIn"/>
          </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
        <DoubleAnimation
          Storyboard.TargetName="LoadingViewBox"
          Storyboard.TargetProperty="Opacity"
          From="1" To="0"
          Duration="0:0:0.5">
          <DoubleAnimation.EasingFunction>
            <CubicEase EasingMode="EaseIn"/>
          </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
        <!-- Texto: slide hacia abajo + fade out con ligero delay -->
        <DoubleAnimation
          Storyboard.TargetName="TextSlide"
          Storyboard.TargetProperty="Y"
          From="0" To="40"
          BeginTime="0:0:0.05"
          Duration="0:0:0.5">
          <DoubleAnimation.EasingFunction>
            <CubicEase EasingMode="EaseIn"/>
          </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
        <DoubleAnimation
          Storyboard.TargetName="LoadingTextBox"
          Storyboard.TargetProperty="Opacity"
          From="1" To="0"
          BeginTime="0:0:0.05"
          Duration="0:0:0.5">
          <DoubleAnimation.EasingFunction>
            <CubicEase EasingMode="EaseIn"/>
          </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
      </Storyboard>
    </Grid.Resources>

    <!-- Loading Image -->
    <Viewbox x:Name="LoadingViewBox" Margin="29.5,0" Grid.Row="1" Grid.Column="2"
             Opacity="0">
      <Image x:Name="LoadingImage" Source="Textures/LoadingImage.png" Width="100" Height="100"
             RenderTransformOrigin="0.5,0.5">
        <Image.RenderTransform>
          <TransformGroup>
            <RotateTransform x:Name="SpinTransform" Angle="0"/>
            <TranslateTransform x:Name="ImageSlide" Y="40"/>
          </TransformGroup>
        </Image.RenderTransform>
        <Image.Triggers>
          <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard>
              <Storyboard>
                <!-- Spin infinito -->
                <DoubleAnimation
                  Storyboard.TargetName="SpinTransform"
                  Storyboard.TargetProperty="Angle"
                  From="360" To="0"
                  Duration="0:0:1"
                  RepeatBehavior="Forever"/>
                <!-- Slide entrada desde abajo -->
                <DoubleAnimation
                  Storyboard.TargetName="ImageSlide"
                  Storyboard.TargetProperty="Y"
                  From="40" To="0"
                  Duration="0:0:0.7">
                  <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                  </DoubleAnimation.EasingFunction>
                </DoubleAnimation>
                <!-- Fade in entrada -->
                <DoubleAnimation
                  Storyboard.TargetName="LoadingViewBox"
                  Storyboard.TargetProperty="Opacity"
                  From="0" To="1"
                  Duration="0:0:0.7">
                  <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                  </DoubleAnimation.EasingFunction>
                </DoubleAnimation>
              </Storyboard>
            </BeginStoryboard>
          </EventTrigger>
        </Image.Triggers>
      </Image>
    </Viewbox>

    <!-- Loading Text -->
    <Viewbox x:Name="LoadingTextBox" Grid.Column="1" Grid.Row="1"
             Opacity="0">
      <TextBlock x:Name="LoadingText" Text="Loading" FontFamily="Arial" FontSize="65" TextAlignment="Center"
                 RenderTransformOrigin="0.5,0.5">
        <TextBlock.RenderTransform>
          <TranslateTransform x:Name="TextSlide" Y="40"/>
        </TextBlock.RenderTransform>
        <TextBlock.Foreground>
          <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
            <GradientStop Offset="0" Color="#FFFFFFFF"/>
            <GradientStop Offset="1" Color="#FFFFFFFF"/>
          </LinearGradientBrush>
        </TextBlock.Foreground>
        <TextBlock.Triggers>
          <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard>
              <Storyboard>
                <!-- Slide entrada desde abajo con delay -->
                <DoubleAnimation
                  Storyboard.TargetName="TextSlide"
                  Storyboard.TargetProperty="Y"
                  From="40" To="0"
                  BeginTime="0:0:0.1"
                  Duration="0:0:0.7">
                  <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                  </DoubleAnimation.EasingFunction>
                </DoubleAnimation>
                <!-- Fade in entrada con delay -->
                <DoubleAnimation
                  Storyboard.TargetName="LoadingTextBox"
                  Storyboard.TargetProperty="Opacity"
                  From="0" To="1"
                  BeginTime="0:0:0.1"
                  Duration="0:0:0.7">
                  <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                  </DoubleAnimation.EasingFunction>
                </DoubleAnimation>
              </Storyboard>
            </BeginStoryboard>
          </EventTrigger>
        </TextBlock.Triggers>
      </TextBlock>
    </Viewbox>

  </Grid>
</Page>